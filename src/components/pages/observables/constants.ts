import type { ObservableType } from '#/components/Observables/observables.types'

export const TYPE_ORDER: ObservableType[] = [
  'domain',
  'url',
  'mail',
  'ip',
  'other',
  'hash',
  'file',
]
